[package]
name = "caster"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.1"
mlua = { version = "0.11.6", features = ["lua55", "vendored"] }
rig-core = "0.31.0"
tokio = { version = "1", features = ["rt-multi-thread"] }
