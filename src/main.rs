use bevy::prelude::*;
use mlua::prelude::*;
use rig::client::{CompletionClient, ProviderClient};
use rig::completion::Prompt;
use rig::providers::openai;

fn main() {
    println!("Hello, world!");
}
